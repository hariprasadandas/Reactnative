rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all operations for testing
    // WARNING: This is completely open - only use for testing!
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 